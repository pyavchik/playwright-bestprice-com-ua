# Base URL of the application under test
BASE_URL=https://bestprice.com.ua

# When TEST_MODE=true, tests may submit forms (do NOT use against production)
# When TEST_MODE=false (default), tests stop before final submit on real-world flows
TEST_MODE=false

# CI flag — set automatically in CI environments
# CI=true

# Optional credentials for authenticated flows (leave empty if not used)
USER_EMAIL=
USER_PASSWORD=
